<%= form_for(@entry, html: { miltipart: true }) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Comment..." %>
  </div>
  <%= f.submit "Comment", class: "btn btn-primary" %>
<% end %>

